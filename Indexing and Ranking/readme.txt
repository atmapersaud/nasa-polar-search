The output is contained in the Query output folder.

All the java source codes are compiled with JAVA-1.7.

For the extra credit, we tried implementing D3 visualization. We got the json file in the required format but since it was very big, the visualization could not be rendered. We attached the d3.json file and d3.html file.